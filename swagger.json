{
  "swagger": "2.0",
  "basePath": "/",
  "info": {
    "title": "STLDevs API",
    "version": "1.0.0"
  },
  "paths": {
    "/devs": {
      "get": {
        "tags": ["Developers"],
        "parameters": [{
          "in": "query",
          "name": "q",
          "type": "string"
        },{
          "in": "query",
          "name": "type",
          "type": "string",
          "enum": ["User", "Organization"]
        }],
        "responses": {
          "default": {
            "schema": {
              "type": "string"
            },
            "description": "Successful"
          }
        }
      }
    }
  }
}
